# myHappybot 개발 지침

- Python 코드는 타입 힌트를 사용하고, 공개 함수에는 간결한 docstring을 작성한다.
- 금융 도메인 로직은 LLM, Streamlit, MCP SDK에 의존하지 않는다.
- DB 접근은 repository 계층에서만 수행하고, MCP 도구는 입력 검증과 서비스 호출만 담당한다.
- 금융 계산은 프롬프트만으로 구현하지 않으며 단위 테스트를 추가한다.
- 의미 있는 변경 뒤에는 관련 테스트를 실행하고, 공개 동작 변경은 README와 `.agent/PLANS.md`에 반영한다.
- 개인 데이터, API 키, SQLite 데이터베이스를 커밋하거나 로그에 출력하지 않는다.

## Travel Mode durable rules

- 여행 지출은 실제 금융 지출이며, 분석 문맥만 일반 지출과 분리한다.
- 환율이나 좌표를 추정하거나 만들어 내지 않는다. 외부 조회 실패 시 지출은 PENDING으로 보존한다.
- 외화 지출은 원금·통화·환율 스냅샷·정산 금액을 보존하고, 환산에는 `Decimal`만 사용한다.
- 여행 예산 알림은 `STRICT` 모드에서만 경고 상태를 만들며, NONE·RELAXED 모드에는 중립적인 정보만 표시한다.
- 위치는 선택 사항이며, 위치 삭제는 지출을 삭제하지 않는다. GPS 추적이나 자동 위치 추론을 추가하지 않는다.
- 외부 환율·장소 provider는 인터페이스 뒤에 두고, 금융 계산은 테스트 가능한 결정론적 코드로 유지한다.
